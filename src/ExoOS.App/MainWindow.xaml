<Window x:Class="ExoOS.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        Title="Exo OS"
        Width="1400" Height="900"
        MinWidth="1100" MinHeight="700"
        WindowStartupLocation="CenterScreen"
        Background="#000000"
        WindowStyle="None"
        ResizeMode="CanResize"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True"
        RenderOptions.BitmapScalingMode="HighQuality"
        RenderOptions.ClearTypeHint="Enabled"
        TextOptions.TextFormattingMode="Display"
        TextOptions.TextRenderingMode="ClearType">

  <!-- Company shell: 52px title strip matches Hub / Launcher / Link -->
  <WindowChrome.WindowChrome>
    <WindowChrome CaptionHeight="0" ResizeBorderThickness="6"
                  GlassFrameThickness="0" UseAeroCaptionButtons="False"/>
  </WindowChrome.WindowChrome>

  <Grid Background="#000000">
    <Grid.RowDefinitions>
      <RowDefinition Height="52"/>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>

    <Border Grid.Row="0"
            Background="#FF000000"
            BorderBrush="#FF161616"
            BorderThickness="0,0,0,1"
            MouseLeftButtonDown="DragStrip_MouseLeftButtonDown">
      <Grid>
        <!-- Brand lockup -->
        <StackPanel Orientation="Horizontal"
                    VerticalAlignment="Center"
                    HorizontalAlignment="Left"
                    Margin="14,0,0,0"
                    IsHitTestVisible="False">
          <Image Source="pack://application:,,,/Assets/exoos.png"
                 Width="28" Height="28"
                 Stretch="Uniform" Margin="0,0,10,0"
                 RenderOptions.BitmapScalingMode="HighQuality"/>
          <StackPanel VerticalAlignment="Center">
            <TextBlock Text="Exo OS"
                       FontSize="13" FontWeight="SemiBold"
                       Foreground="#FFF2F2F2"
                       FontFamily="Segoe UI Variable Text, Segoe UI"
                       LineHeight="15"/>
            <TextBlock Text="Transform"
                       FontSize="10" FontWeight="Medium"
                       Foreground="#FF555555"
                       FontFamily="Segoe UI Variable Text, Segoe UI"
                       LineHeight="12"/>
          </StackPanel>
        </StackPanel>

        <!-- Drag hit area between brand and buttons -->
        <Border Background="Transparent" Margin="160,0,90,0"/>

        <!-- Window cluster: minimize + close (company standard) -->
        <StackPanel Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Center"
                    Margin="0,0,8,0">
          <Button Width="36" Height="32" Margin="0,0,2,0"
                  Click="Minimize_Click" Cursor="Hand"
                  ToolTip="Minimize"
                  WindowChrome.IsHitTestVisibleInChrome="True"
                  Content="−"
                  FontSize="16"
                  Style="{StaticResource WinChromeBtn}"/>
          <Button Width="36" Height="32"
                  Click="Close_Click" Cursor="Hand"
                  ToolTip="Close"
                  WindowChrome.IsHitTestVisibleInChrome="True"
                  Content="×"
                  FontSize="15"
                  Style="{StaticResource WinChromeCloseBtn}"/>
        </StackPanel>
      </Grid>
    </Border>

    <Grid Grid.Row="1">
      <wv2:WebView2 x:Name="Web"
                    DefaultBackgroundColor="#000000"
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Stretch"/>

      <StackPanel x:Name="BootPanel"
                  HorizontalAlignment="Center"
                  VerticalAlignment="Center"
                  IsHitTestVisible="False">
        <TextBlock Text="Starting Exo OS…"
                   Foreground="#FF8A8A8A"
                   FontSize="13"
                   HorizontalAlignment="Center"
                   FontFamily="Segoe UI Variable Text, Segoe UI"/>
      </StackPanel>
    </Grid>
  </Grid>
</Window>
