<Window x:Class="ExoOS.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:wv2="clr-namespace:Microsoft.Web.WebView2.Wpf;assembly=Microsoft.Web.WebView2.Wpf"
        Title="Exo OS"
        Width="1400" Height="900"
        MinWidth="1100" MinHeight="700"
        WindowStartupLocation="CenterScreen"
        Background="#000000"
        WindowStyle="None"
        ResizeMode="CanResize"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True"
        RenderOptions.BitmapScalingMode="HighQuality"
        RenderOptions.ClearTypeHint="Enabled"
        TextOptions.TextFormattingMode="Display"
        TextOptions.TextRenderingMode="ClearType">

  <!-- Thin native drag strip above WebView (no Home/Settings). WebView cannot drag alone. -->
  <WindowChrome.WindowChrome>
    <WindowChrome CaptionHeight="0" ResizeBorderThickness="6"
                  GlassFrameThickness="0" UseAeroCaptionButtons="False"/>
  </WindowChrome.WindowChrome>

  <Grid Background="#000000">
    <Grid.RowDefinitions>
      <RowDefinition Height="36"/>
      <RowDefinition Height="*"/>
    </Grid.RowDefinitions>

    <!-- Ecosystem chrome: product mark + title + close (matches Hub / Launcher language) -->
    <Border Grid.Row="0"
            Background="#FF000000"
            BorderBrush="#FF161616"
            BorderThickness="0,0,0,1"
            MouseLeftButtonDown="DragStrip_MouseLeftButtonDown">
      <Grid>
        <StackPanel Orientation="Horizontal"
                    VerticalAlignment="Center"
                    Margin="14,0,0,0"
                    IsHitTestVisible="False">
          <Border Width="18" Height="18" CornerRadius="5" Background="#FF0C0C0C"
                  BorderBrush="#FF222222" BorderThickness="1" Margin="0,0,10,0">
            <TextBlock Text="E" FontSize="10" FontWeight="SemiBold"
                       Foreground="#FFF2F2F2"
                       HorizontalAlignment="Center" VerticalAlignment="Center"
                       FontFamily="Segoe UI Variable Text, Segoe UI"/>
          </Border>
          <TextBlock Text="Exo OS" FontSize="12" FontWeight="SemiBold"
                     Foreground="#FFF2F2F2" VerticalAlignment="Center"
                     FontFamily="Segoe UI Variable Text, Segoe UI"/>
          <TextBlock Text="Transform" FontSize="11" Margin="10,0,0,0"
                     Foreground="#FF555555" VerticalAlignment="Center"
                     FontFamily="Segoe UI Variable Text, Segoe UI"/>
        </StackPanel>
        <Border Background="Transparent" Margin="0,0,48,0"/>
        <Button HorizontalAlignment="Right" VerticalAlignment="Center"
                Margin="0,0,6,0" Width="40" Height="28"
                Click="Close_Click" Cursor="Hand"
                ToolTip="Close"
                WindowChrome.IsHitTestVisibleInChrome="True"
                Background="Transparent" BorderThickness="0" Foreground="#FF8A8A8A"
                FontSize="14" Content="×"
                Style="{StaticResource WinChromeCloseBtn}"/>
      </Grid>
    </Border>

    <Grid Grid.Row="1">
      <wv2:WebView2 x:Name="Web"
                    DefaultBackgroundColor="#000000"
                    HorizontalAlignment="Stretch"
                    VerticalAlignment="Stretch"/>

      <StackPanel x:Name="BootPanel"
                  HorizontalAlignment="Center"
                  VerticalAlignment="Center"
                  IsHitTestVisible="False">
        <TextBlock Text="Starting Exo OS…"
                   Foreground="#FF8A8A8A"
                   FontSize="13"
                   HorizontalAlignment="Center"
                   FontFamily="Segoe UI Variable Text, Segoe UI"/>
      </StackPanel>
    </Grid>
  </Grid>
</Window>
